{
  "name": "symposion-functions",
  "description": "Firebase Cloud Functions for SymposiON — Stripe payments & admin",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "mocha test/rules.test.js --timeout 30000"
  },
  "engines": {
    "node": ">=22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^7.2.5",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "mocha": "^10.0.0"
  },
  "private": true
}
