node_modules/
.DS_Store
Thumbs.db
*.log
.cache/
.playwright/

# Claude agent worktrees / internals
.claude/

# Unity WebGL build artifacts (large binaries — host with the app or a CDN,
# don't commit them). Keep the folder itself via .gitkeep.
synthesis/games/unity/Build/*
!synthesis/games/unity/Build/.gitkeep
