node_modules/
.DS_Store
Thumbs.db
*.log
.cache/
.playwright/

# Secrets — real Cloud Functions env (ANTHROPIC_KEY etc.). Never commit these;
# only functions/.env.example (the template) is tracked.
.env
.env.local
.env.*.local
**/.env

# Claude agent worktrees / internals
.claude/
.firebase/
